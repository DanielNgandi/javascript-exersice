
let randomNumbers = Math.floor(Math.random());
let userGuess = prompt("Guess a number between 1 and 10:");
    if (randomNumbers === userGuess && randomNumbers !==randomNumbers ){
        alert('Good work')
    }
    else{
        alert('Not matched')
    }

